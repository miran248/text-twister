import { Id } from "./index";

describe("index/Id", () => {
  it("is defined", () => {
    expect(Id).toBeDefined();
  });

  // it("should return Uint8Array instance", () => {
  //   expect(Id(encoded)).toBeInstanceOf(Uint8Array);
  // });
});

// describe("index/toHex", () => {
//
// });
